@model IEnumerable<NoNameBikes.Models.AccessoriesListModel>

@{
    ViewData["Title"] = "Pumps";
}

<h1>Pumps</h1>

<table class="table">
    @{
        foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink(item.ProductModel, "Details", new { id = item.ProductModelID })
                </td>
                <td>
                    @item.Description
                </td>

            </tr>
        }
    }
</table>